<% unless params[:list_category_id].nil? %>
    <%= render 'admin/lists/menu', category: listing.category, listing_active: listing_active %>
<% end %>
<% unless params[:list_entity_id].nil? %>
    <%= render 'admin/list_entities/menu', entity: listing.entity, listing_active: listing_active %>
<% end %>
