<%= render 'admin/lists/menu' %>
<section>
    <h1><%= image_tag 'admin/icons/actions/show-black.png' %> Category: <%= @category.name %></h1>
    <p class="big"><%= @category.description %></p>

    <h2><%= image_tag 'admin/icons/actions/index-black.png' %> Listings</h2>
    <p>This category currently has <%= @category.listings.count %> listings.</p>
    <div>
    </div>
</section>